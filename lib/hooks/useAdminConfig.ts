
// This is a placeholder hook for a feature that is not fully implemented.
export const useAdminConfig = () => {
    return {
        data: { featureFlags: { newOnboarding: false } },
        isLoading: false,
        isError: false,
    };
};
